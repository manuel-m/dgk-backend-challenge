{
  "name": "digikare-vanilla",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node build.mjs",
    "setup": "node setup.mjs",
    "dev": "node setup.mjs && node build.mjs --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.47"
  },
  "dependencies": {
    "ajv": "^8.11.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "postgres": "^3.2.4"
  }
}
